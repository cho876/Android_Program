<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cho.account_book">

    <application
        android:name=".CustomFont"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CustomBtn" />
        <activity android:name=".CustomEdit" />
        <activity android:name=".CustomImgBtn" />
        <activity android:name=".JoinActivity" />
        <activity android:name=".CustomTitle" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".FindIDActivity" />
        <activity android:name=".FindPWActivity" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>